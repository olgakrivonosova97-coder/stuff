# Отчёт о внесённых правках — presentation/

Дата: 2026-07-09. Правки внесены по подтверждённому плану
(`analysis/03_edit_plan_confirmed.md`). Редизайн не выполнялся; палитра, шрифты,
сетка и анимации не менялись.

## Изменённые файлы

- `presentation/index.html` — единственный изменённый файл (39 вставок / 38 удалений).
- `presentation/styles.css` — **не изменялся** (правки не потребовали CSS).
- `presentation/script.js` — **не трогался** (навигация построена динамически по `.slide` и `data-title`, переупорядочивание секций подхватывается автоматически).

## Изменённые слайды

1. **`slide-limits`** (Ограничения контура) — карточка «Цифровой помощник».
   Конфликтная формулировка «отвечает пока только в части урегулирования долга…»
   полностью заменена на:
   «База знаний расширяется: приоритет — урегулирование долга; дополнительно
   загружены материалы по смежным направлениям. Неактуальные НПА помечены.»
   Старая формулировка не оставлена (проверено: вхождений «только в части
   урегулирования долга» — 0).

2. **`slide-work`** (Организация работы) — к пояснению под блоком 80/20 добавлено:
   «Соотношение 80/20 — экспертная оценка команды проекта по итогам практической
   отладки.» Внешняя ссылка/закон Парето не добавлялись.

3. **`slide-partners`** (Партнёры и экспертиза) — в lead добавлена безопасная
   формулировка «По результатам первичного анализа рынка релевантных готовых
   решений не выявлено.» Числа 500/576 не использованы.

4. **Блок проблем — переупорядочивание (контур → проблема №2):**
   - Слайд `slide-infra` физически перемещён и теперь идёт сразу после
     `slide-arch`. Эйбрау изменён: «02·5 · Инфраструктура и контуры» →
     «02·2 · Контур и инфраструктура». Контент слайда (три контура, доступ через
     ФКУ, парк GPU 20×H200/+88×H200/108, ~90 карт, ТЭО) сохранён без изменений —
     инфраструктура/GPU остались отдельным акцентом.
   - Эйбрау сдвинутых проблем перенумерованы: Организация работы 02·2 → **02·3**,
     Модели 02·3 → **02·4**, Данные 02·4 → **02·5**; Партнёры остались **02·6**.
   - Обзорный слайд `slide-prob-map`: карточки переупорядочены и перенумерованы —
     1 Архитектура · 2 Контур и инфраструктура · 3 Организация работы · 4 Модели ·
     5 Данные · 6 Партнёры и экспертиза.

   Итоговый порядок секций (16 слайдов, без добавления/удаления):
   hero → passport → status → prob-map → **arch → infra** → work → models → data →
   partners → demo → limits → lowcode → plan26 → plan27 → final.

## Закрытые замечания

- К1 — конфликт по базе знаний ЦП: закрыт (нейтральная формулировка, вариант А).
- К2 — 80/20: закрыт (оформлено как экспертная оценка команды, без источника).
- К3 — исследование рынка: закрыт (формулировка без чисел на `slide-partners`).
- К4 — контур vs инфраструктура: закрыт (контур = проблема №2, без 7-й проблемы,
  без редизайна).
- К6 — рамка показа «платформа → сервис»: изменений не требовалось (фраза уже была
  в lead `slide-demo`; отдельная плашка по решению не добавлялась).

## Отложенные замечания

- К5 — блок «Зоны внимания» / слайд рисков: не вносился (по вашему решению),
  остаётся в отложенных.
- «Серые зоны» (широкое экспертное сообщество, возврат к рискам) — намеренно не
  выносятся на слайды; держим как контекст для устного выступления.

## Проверка

- Локальный запуск: `python3 -m http.server` — `index.html` отдаётся (HTTP 200),
  шрифты и Font Awesome грузятся локально.
- Все 16 слайдов присутствуют; счётчик показывает «1 / 16».
- Навигация работает: точки (клик по 5-й точке → «5 / 16»), стрелки, клавиатура,
  прогресс-бар — script.js не менялся.
- Сделаны скриншоты изменённых слайдов (обзор проблем, контур/инфра, организация
  работы, партнёры, ограничения контура) — вёрстка корректна.

## Изменения в CSS

Нет. `styles.css` не изменялся.

## Риск визуальной поломки

Низкий. Все правки — только текст и порядок существующих секций/карточек в
`index.html`; новые классы, стили и элементы разметки не вводились. Используются
те же компоненты (`.card`, `.stand-card`, `.stat`, `.case-note`), что и раньше.
Единственный нюанс — на скриншоте `slide-limits` нижние карточки выглядят
приглушёнными: это штатная reveal-анимация (fade-in по задержке), а не дефект
вёрстки.
